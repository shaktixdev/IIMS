import UnitForm from "@/components/settings/UnitForm";

export const metadata = {
  title: "Units of Measurement | IIMS",
  description: "Configure weight, count, length, volume, and conversions factors",
};

export default function UnitsSettingsPage() {
  return <UnitForm />;
}
